language: ruby
rvm:
  - 2.7.2
services:
  - postgresql
before_script:
  - psql -c "CREATE USER cashflow_user WITH PASSWORD 'cashflow_pass';" -U postgres
  - psql -c "ALTER USER cashflow_user CREATEDB;" -U postgres
  - bin/rails db:setup
